# Example environment variables for local dev.
# Copy to backend/.env and fill values.

# Postgres (docker compose from repo root). Copy to backend/.env:
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/petsit

